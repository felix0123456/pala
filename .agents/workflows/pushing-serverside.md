---
description: 
---

After changing any code for the server, push to github
The server automatically hosts the app through coolify to pala.felixresch.com
there is a persistent storage at /app/data

Always work on the latest firmware version for the pala.

For any code changes decide based on the severity of the change wether to iterate the last or middle version number.
If asked to "upload" or "push", push to github with origin v*.*.*